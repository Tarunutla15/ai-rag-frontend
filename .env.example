# Optional: backend URL for production builds (no trailing slash).
# Leave unset for local dev — Vite proxies /chat, /upload, etc. to http://127.0.0.1:8000
#
# VITE_API_BASE_URL=https://rag-backend-k0ov.onrender.com
